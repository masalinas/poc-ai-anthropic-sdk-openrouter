# Description
PoC use Anthopic SDK with Openrouter

## Dependencies
```bash
$ pip install anthropic
```

## Steps
We can use openroute. We need a API_KEY. But in this case from SDK we can use the claude models. But be carefull, all claude models are payable, so you must add credits in to your openrouter account or use the free ones as usual.
